# OEIS draft — noncappable change-ringing sequences, 4 bells.
# Staged 2026-06-15.  NOT paste-ready and NOT for automated submission:
# OEIS forbids AI-authored submissions, so this file stages the computation
# and its references only. See oversight/oeis/README.md for the routing.
# Reproduce/verify all terms:  node oversight/oeis/noncappable-change-ringing/verify.mjs  (covers every b-file term)
# This sequence = A324943 (path) minus A324942 (cyclic), term by term;
# independently recomputed by direct DFS (engine.mjs) for L<=24;
# beyond that the terms are the published difference, and past the
# published range they are the extension counter's, drift-guarded
# against expected-extension.mjs. Corrected 2026-07-28: the earlier
# reach printed here was retracted by the 2026-07-20 coverage audit.

%S 0,0,6,24,72,182,436,1016,2364,5390,11912,25382,51668,99802,181616,
%T 308254,483016,688220,865808,919988,781880,497180,213264,48156
%N Number of noncappable change-ringing sequences of length n for 4 bells.
%C A change-ringing sequence of length k for 4 bells is a sequence of k distinct rows (permutations of 1..4) beginning at rounds (1,2,...,4) in which every row differs from the previous one by a "change": each bell stays put or moves one position, i.e. the rows differ by a product of disjoint adjacent transpositions (see J. K. Sonsteby's definition, the source of A324942-A324949). Such a sequence is "cyclic" if its last row can return to rounds in one further change (A324942), and "path" if no closure is required (A324943). It is NONCAPPABLE when its last row is NOT one change away from rounds, so the sequence cannot be capped to close into a cyclic touch. Equivalently a(n) = (path sequences of length n) - (cyclic sequences of length n) = A324943(n) - A324942(n).
%C In graph terms: rows are the 4! vertices of the change-ringing graph on 4 bells (a Cayley graph of the symmetric group S_4 generated by the nonempty products of disjoint adjacent transpositions; its degree is A000071(4+1) = Fibonacci(4+1)-1). a(L) counts simple paths of L vertices that start at the identity and END at a vertex that is NOT adjacent to the identity.
%C a(1) = a(2) = 0: a single row, or rounds plus any one neighbour, is always cappable. Leading zeros are kept so that a(L) is indexed by sequence length L, matching the sibling sequences A324942 and A324943.
%C For 4 bells the change-ringing graph is the truncated octahedron; the sequence is complete (the longest sequence has length 4! = 24).
%H J. K. Sonsteby, <a href="https://github.com/jonassonsteby/change-ringing">change-ringing (Python program; defines cyclic, path and noncappable sequences)</a>.
%H <a href="/index/Be#bell_ringing">Index entries for sequences related to bell ringing</a>
%e For 4 bells the shortest noncappable sequence has length 3: rounds, then two changes ending on a row that cannot reach rounds in one change; a(3) = 6.
%Y Cf. A324942 (cyclic, 4 bells), A324943 (path, 4 bells), A000071 (number of changes = Fibonacci(n+1)-1). Sequences for other bell counts: 4,5,6,7 bells (this family).
%K nonn,fini,full
%O 1,3
%A [submitter]
