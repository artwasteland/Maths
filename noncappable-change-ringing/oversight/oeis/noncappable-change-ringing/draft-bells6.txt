# OEIS draft — noncappable change-ringing sequences, 6 bells.
# Staged 2026-06-15.  NOT paste-ready and NOT for automated submission:
# OEIS forbids AI-authored submissions, so this file stages the computation
# and its references only. See oversight/oeis/README.md for the routing.
# Reproduce/verify all terms:  node oversight/oeis/noncappable-change-ringing/verify.mjs  (covers every b-file term)
# This sequence = A324947 (path) minus A324946 (cyclic), term by term;
# independently recomputed by direct DFS (engine.mjs) for L<=9;
# beyond that the terms are the published difference, and past the
# published range they are the extension counter's, drift-guarded
# against expected-extension.mjs. Corrected 2026-07-28: the earlier
# reach printed here was retracted by the 2026-07-20 coverage audit.

%S 0,0,72,1028,12308,134784,1412536,14499434,147458504,1492968670,
%T 15073553266,151836540344,1526242691836
%N Number of noncappable change-ringing sequences of length n for 6 bells.
%C A change-ringing sequence of length k for 6 bells is a sequence of k distinct rows (permutations of 1..6) beginning at rounds (1,2,...,6) in which every row differs from the previous one by a "change": each bell stays put or moves one position, i.e. the rows differ by a product of disjoint adjacent transpositions (see J. K. Sonsteby's definition, the source of A324942-A324949). Such a sequence is "cyclic" if its last row can return to rounds in one further change (A324946), and "path" if no closure is required (A324947). It is NONCAPPABLE when its last row is NOT one change away from rounds, so the sequence cannot be capped to close into a cyclic touch. Equivalently a(n) = (path sequences of length n) - (cyclic sequences of length n) = A324947(n) - A324946(n).
%C In graph terms: rows are the 6! vertices of the change-ringing graph on 6 bells (a Cayley graph of the symmetric group S_6 generated by the nonempty products of disjoint adjacent transpositions; its degree is A000071(6+1) = Fibonacci(6+1)-1). a(L) counts simple paths of L vertices that start at the identity and END at a vertex that is NOT adjacent to the identity.
%C a(1) = a(2) = 0: a single row, or rounds plus any one neighbour, is always cappable. Leading zeros are kept so that a(L) is indexed by sequence length L, matching the sibling sequences A324946 and A324947.
%H J. K. Sonsteby, <a href="https://github.com/jonassonsteby/change-ringing">change-ringing (Python program; defines cyclic, path and noncappable sequences)</a>.
%H <a href="/index/Be#bell_ringing">Index entries for sequences related to bell ringing</a>
%e For 6 bells the shortest noncappable sequence has length 3: rounds, then two changes ending on a row that cannot reach rounds in one change; a(3) = 72.
%Y Cf. A324946 (cyclic, 6 bells), A324947 (path, 6 bells), A000071 (number of changes = Fibonacci(n+1)-1). Sequences for other bell counts: 4,5,6,7 bells (this family).
%K nonn,fini,more
%O 1,3
%A [submitter]
