# OEIS draft — noncappable change-ringing sequences, 5 bells.
# Staged 2026-06-15.  NOT paste-ready and NOT for automated submission:
# OEIS forbids AI-authored submissions, so this file stages the computation
# and its references only. See oversight/oeis/README.md for the routing.
# Reproduce/verify all terms:  node oversight/oeis/noncappable-change-ringing/verify.mjs  (covers every b-file term)
# This sequence = A324945 (path) minus A324944 (cyclic), term by term;
# independently recomputed by direct DFS (engine.mjs) for L<=12;
# beyond that the terms are the published difference, and past the
# published range they are the extension counter's, drift-guarded
# against expected-extension.mjs. Corrected 2026-07-28: the earlier
# reach printed here was retracted by the 2026-07-20 coverage audit.

%S 0,0,24,184,1144,6358,33980,180420,956230,5035566,26295718,
%T 136321728,702968774,3609843894,18463217022,94037662576,
%U 476843876534,2406990346480
%N Number of noncappable change-ringing sequences of length n for 5 bells.
%C A change-ringing sequence of length k for 5 bells is a sequence of k distinct rows (permutations of 1..5) beginning at rounds (1,2,...,5) in which every row differs from the previous one by a "change": each bell stays put or moves one position, i.e. the rows differ by a product of disjoint adjacent transpositions (see J. K. Sonsteby's definition, the source of A324942-A324949). Such a sequence is "cyclic" if its last row can return to rounds in one further change (A324944), and "path" if no closure is required (A324945). It is NONCAPPABLE when its last row is NOT one change away from rounds, so the sequence cannot be capped to close into a cyclic touch. Equivalently a(n) = (path sequences of length n) - (cyclic sequences of length n) = A324945(n) - A324944(n).
%C In graph terms: rows are the 5! vertices of the change-ringing graph on 5 bells (a Cayley graph of the symmetric group S_5 generated by the nonempty products of disjoint adjacent transpositions; its degree is A000071(5+1) = Fibonacci(5+1)-1). a(L) counts simple paths of L vertices that start at the identity and END at a vertex that is NOT adjacent to the identity.
%C a(1) = a(2) = 0: a single row, or rounds plus any one neighbour, is always cappable. Leading zeros are kept so that a(L) is indexed by sequence length L, matching the sibling sequences A324944 and A324945.
%H J. K. Sonsteby, <a href="https://github.com/jonassonsteby/change-ringing">change-ringing (Python program; defines cyclic, path and noncappable sequences)</a>.
%H <a href="/index/Be#bell_ringing">Index entries for sequences related to bell ringing</a>
%e For 5 bells the shortest noncappable sequence has length 3: rounds, then two changes ending on a row that cannot reach rounds in one change; a(3) = 24.
%Y Cf. A324944 (cyclic, 5 bells), A324945 (path, 5 bells), A000071 (number of changes = Fibonacci(n+1)-1). Sequences for other bell counts: 4,5,6,7 bells (this family).
%K nonn,fini,more
%O 1,3
%A [submitter]
